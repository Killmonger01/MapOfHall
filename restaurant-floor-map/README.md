# Карта зала ресторана

Интерактивная карта зала ресторана с возможностью панорамирования, масштабирования и просмотра деталей столов.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение откроется по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

### Предварительный просмотр продакшен-сборки

```bash
npm run preview
```

## 📁 Структура проекта

```
restaurant-floor-map/
├── src/
│   ├── components/          # UI компоненты
│   │   ├── FloorMap.tsx    # Главный компонент карты
│   │   ├── FloorBackground.tsx  # Фон и зоны зала
│   │   ├── TableItem.tsx   # Отдельный стол
│   │   └── TableDetailsPanel.tsx  # Панель деталей стола
│   ├── models/             # Бизнес-логика
│   │   └── TransformModel.ts  # Модель трансформации (pan/zoom)
│   ├── lib/                # Утилиты и хуки
│   │   ├── coordinates.ts  # Преобразование координат
│   │   └── useMapInteraction.ts  # Хук для взаимодействия с картой
│   ├── data/               # Данные
│   │   └── tables.ts       # Данные столов
│   ├── types/              # TypeScript типы
│   │   └── index.ts        # Все типы приложения
│   ├── App.tsx             # Корневой компонент
│   └── main.tsx            # Entry point
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## 🏗️ Архитектура и ключевые решения

### Разделение ответственности

Проект следует принципу разделения на слои:

#### 1. **Слой данных** (`/data`)
- Хранит локальные данные столов
- Конфигурация карты зала
- Все координаты и размеры задаются в данных

#### 2. **Слой моделей** (`/models`)
- `TransformModel` - управляет трансформацией карты (pan/zoom)
- Содержит всю логику ограничений масштабирования и панорамирования
- Не зависит от UI

#### 3. **Слой утилит** (`/lib`)
- `coordinates.ts` - преобразование координат между системами (экран ↔ карта)
- `useMapInteraction.ts` - хук для обработки событий мыши
- Разделяет клики и drag-события (drag не приводит к выбору стола)

#### 4. **Слой UI** (`/components`)
- Компоненты только отображают данные и вызывают обработчики
- Минимальная логика в компонентах

### Технические решения

#### Рендеринг: SVG
Выбран SVG вместо Canvas по следующим причинам:
- Более простая работа с событиями (нативные DOM события)
- Автоматическое масштабирование через transform
- Лучшая читаемость кода
- Векторная графика для четкости при масштабировании

#### Управление трансформацией
`TransformModel` инкапсулирует всю логику:
- Ограничения масштаба (0.5x - 3x)
- Динамические границы панорамирования (зависят от масштаба)
- Масштабирование относительно точки курсора

#### Разделение клика и drag
Реализован порог перемещения (`DRAG_THRESHOLD = 5px`):
- Если перемещение < 5px → обрабатывается как клик
- Если перемещение ≥ 5px → обрабатывается как drag
- Предотвращает случайный выбор при панорамировании

#### Преобразование координат
Двунаправленное преобразование:
- `screenToMap` - для определения кликов
- `mapToScreen` - для отрисовки (не используется явно благодаря SVG transform)

## 🎮 Функциональность

### Реализовано

✅ **Отрисовка карты**
- План зала с 4 зонами (VIP, Main Hall, Terrace, Bar)
- 14 столов различной формы (круглые и прямоугольные)
- Визуальное разделение зон

✅ **Перемещение по карте**
- Панорамирование: перетаскивание мышью
- Масштабирование: колесо мыши (0.5x - 3x)
- Ограничения на масштаб и перемещение

✅ **Взаимодействие**
- Клик по столу выбирает его
- Визуальное выделение выбранного стола (синяя обводка)
- Клик по пустому месту снимает выбор
- Разделение клика и drag

✅ **Панель деталей**
- Открывается при выборе стола
- Показывает: статус, вместимость, секцию, форму, размеры
- Закрывается кнопкой или при снятии выбора

✅ **Дополнительно**
- Панель управления с кнопкой сброса вида
- Индикатор текущего масштаба
- Легенда статусов столов
- Подсказки по использованию

### Данные столов

Каждый стол содержит:
```typescript
{
  id: string;           // Уникальный идентификатор
  name: string;         // Название (номер)
  x, y: number;         // Координаты
  width, height: number; // Размеры
  shape: 'rectangle' | 'circle'; // Форма
  capacity: number;     // Вместимость
  status: 'available' | 'occupied' | 'reserved'; // Статус
  section: string;      // Секция зала
}
```

## 🎨 Цветовая схема

- **Доступен** (available): зеленый (#4ade80)
- **Забронирован** (reserved): желтый (#fbbf24)
- **Занят** (occupied): красный (#f87171)
- **Выбран**: синяя обводка (#3b82f6)

## 📝 Возможные улучшения

### Функциональные
1. **Множественный выбор столов** - с клавишей Ctrl/Cmd
2. **Поиск столов** - по номеру, секции, статусу
3. **Фильтрация** - показывать только доступные/занятые столы
4. **История выбора** - навигация назад/вперед
5. **Экспорт плана** - в PNG/PDF
6. **Редактор карты** - перемещение столов, изменение размеров
7. **Мини-карта** - обзор всего зала с индикатором текущего вида

### Технические
1. **Canvas рендеринг** - для больших залов (100+ столов)
2. **Виртуализация** - рендерить только видимые столы
3. **Анимации** - плавные переходы при масштабировании
4. **Тач-события** - поддержка мобильных устройств
5. **Клавиатурная навигация** - стрелки для панорамирования, +/- для зума
6. **Сохранение состояния** - позиция и масштаб в localStorage
7. **Тесты** - unit-тесты для моделей и утилит

### UX/UI
1. **Темная тема**
2. **Адаптивная верстка** - оптимизация для планшетов/телефонов
3. **Контекстное меню** - ПКМ на столе для быстрых действий
4. **Подсказки** - tooltips при наведении на стол
5. **Статистика** - счетчики доступных/занятых столов
6. **Времена бронирования** - визуализация расписания на временной шкале

### Интеграция
1. **WebSocket** - обновление статусов в реальном времени
2. **REST API** - CRUD операции со столами
3. **Бронирование** - интеграция с системой резервирования
4. **Аналитика** - тепловая карта популярности столов
5. **Экспорт данных** - CSV, JSON

## 🛠️ Технологический стек

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **SVG** - рендеринг карты

## 📄 Лицензия

MIT
